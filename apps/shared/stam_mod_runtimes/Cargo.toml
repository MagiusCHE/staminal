[package]
name = "stam_mod_runtimes"
version = "0.1.0"
edition = "2024"

[dependencies]
# Core dependencies
tracing = "0.1"

# Optional runtime-specific dependencies
rquickjs = { version = "0.6", features = ["classes", "properties", "loader"], optional = true }

[features]
default = []
# Enable JavaScript runtime support
js = ["rquickjs"]
# Future runtime support
lua = []
csharp = []
